# Base test resources root; profile-specific values live in application-test.yml.
# Quarkus loads application-test.yml when the test profile is active (@QuarkusTest).
quarkus:
  profile: test
  devservices:
    enabled: true
  datasource:
    devservices:
      image-name: postgres:18
